# Repo-root ignores. Note that 03_App/web/.gitignore still applies to that subtree —
# git resolves leading-slash patterns relative to the .gitignore that declares them,
# so /node_modules in there correctly means 03_App/web/node_modules.
.DS_Store
*.log
